name: movilizer
channels:
  - conda-forge
  - pytorch
dependencies:
  - python=3.10
  - pip>=24
  - ffmpeg
  - numpy>=1.24
  - scipy>=1.10
  - pyyaml>=6.0
  - pillow>=10.0
  - pip:
      - -r requirements.txt
